services:
  web:
    build: .
    ports:
      - "5180:5180"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=5180
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:5180"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
